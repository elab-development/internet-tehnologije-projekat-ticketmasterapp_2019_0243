export enum Pages {
  HOME,
  EVENTS,
  ADMIN_TABLE,
  VENUE_TABLE,
  EMPLOYEE_TABLE,
  ABOUT,
  AUTH,
  REGISTER,
  TICKETS,
  RESET,
}

export enum RoleNameEnum {
  ADMIN = "admin",
  USER = "user",
  GUEST = "guest",
}
